

const firstName = "Godwin";
let age = 25;
const country = "Nigeria";

console.log(firstName);
console.log(age);
console.log(country);


// Change the age if needed.

// Run it in your terminal by typing: **node index.js**
// ensure you are in the correct directory before running the command. you can run cd 01-variables to change to the correct directory.
// You should see three lines printed in your terminal.


// # Exercise 

// In `exercises.js`, 

// ---

// 💡 Did You Know?

// Variables don't actually "contain" values. A better mental model is that they **reference** values in memory. We'll explore memory and references later, but it's useful to know that JavaScript is managing data behind the scenes for you.
